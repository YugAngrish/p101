
def dice(response):
    import random 
    y = response
    while(response == y):
      no = random.randint(1,6)
      print(y)

    n = response 
    while(response == n):
        print("Project ended")
    print("Press y to cintinue and n to exit")

